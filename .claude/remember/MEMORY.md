# Project Memory
> Auto-generated by /remember. Do not edit manually.

## Facts
- Rung-1 M0/M1/M1b/M2 all done; M2 shipped as v0.5.0 (chiproof 0.5.0 on npm), freeze lifted (D65); M3 (tier A/B, mode C deferred) now building.
- `grep` on this host is ugrep 7.5.0 — bounded-repetition regexes fail slowly and look like a hang; parse structured data with python3 instead.
- Never write custom passport-chip parsing code — wrap a vetted library (NFCPassportReader on iOS, JMRTD on Android/desktop).
- zkagent is Android-first: builds on the owner's Fedora box, no Mac/paid Apple account needed; iOS is an explicit non-goal for now.
- zkagent's Android dev device: Pixel, stock ROM, bootloader locked — StrongBox/Play Integrity/patch runway decide it; never GrapheneOS, FRP-locked, or 3a/4a units.
- CPU/secure-enclave material (raw attestation chains too) never enters zktag/identity derivation — roots custody only; a raw chain is a stable per-device ID, breaks unlinkability.
- Evidence-plug/verifier contracts must never throw: `ok:false` forces `allowed:null`, `valid:false` forces `allowed:false` — keep both distinctions everywhere.
- Real passport material and PII never enter git or docs/reports — keep `fixtures/real/`, `*.mrz`, `*.dg1`, `*.sod`, `secrets/` gitignored; record field names, verdicts, timings.
- "Commit" is complete on its own — never chain `/branch-review`/`/release`/`/refactor` after it; only the owner decides when a branch is done.
- Don't blind-replace `tag`→`zktag` — Apple's "NFC tag-reading entitlement" and RFC 9421's own `tag` param must stay as `tag`.
- zkagent npm package is dropped entirely — only `chiproof` ships from this project; `zkagent@0.0.0` stays published, parked, MIT-mislicensed, not being re-raised.
- `main`'s branch protection (1 review + linear history) can't be met solo — GitHub blocks self-approval, forcing admin bypass each merge; flag every bypass, never assume consent.
- When a stale/broken reference is found after a docs reorg, fix it on sight or flag it — do not assume it's already correct.
- zkagent's agent-delegation layer (rung 2, M4-M5) is frozen until age-verification (rung 1) ships — don't design/discuss it, even if asked casually.
- No feature enters a zkagent milestone unless it is first written into the PRD (NO-GO #10, the scope gate).
- Main/orchestrator session never codes/edits docs — spawn an agent with `model: sonnet` explicit (else it inherits a different model); escalate design decisions back.
- Every module's build starts with a POC on real devices/documents aimed at its riskiest assumption, before the easy parts get built.
- Play Integrity tokens are non-transferable/per-app-quota, never a shared voucher — why chiproof uses a pluggable evidence-slot design, not fixed attestation.
- X509 chain verification checks signatures only, not validity dates — always check `notBefore`/`notAfter` explicitly, don't trust `verify()` alone.
- A ZK circuit's public inputs can bind a fresh nonce or expose a stable nullifier, never both — a proof is either challenge-bound or a persistent pseudonym.
- After `/clear`, background agents keep running — check `ListAgents` AND wait for a running agent's reply before spawning a new one for the same task, or two agents race.
- An npm 404 on `PUT` under OIDC has two causes — workflow filename mismatch, or no trusted-publisher binding for that package at all; diagnose which, don't assume.
- Real device/API captures parse differently from synthetic fixtures (wrapper layers, log prefixes) — validate parsers against a real capture, not synthetic data alone.
- No ZK circuit built in this project is "ours" — third-party ZK is a validation-grade evidence plug only; never market v1 as zero-knowledge.
- Worktree isolation branches from main, not the caller's current branch — state the exact rebase/fast-forward target SHA when spawning.
- Run a coder in a worktree while a review holds the main checkout, then fast-forward the coder's commits in after.
- `.claude/remember`/`.claude/stash` stay tracked (§7 exemption), except `last-review.md`/`fix-ledger.md` — gitignored since tracking them made `/branch-review` invalidate itself.
- Verify a hardware/vendor claim (protocol limits, compatibility) against the actual use case, not against a loosely related signal like "works for a similar product."
- Design-only sessions accumulate unresolved surface faster than they resolve it — writing a design's data/read path out concretely surfaces risks that argument alone misses.
- Mode-B individuals are distinguished by `document_number`, not a derived hash of the full MRZ or DG1.
- Disclose known cross-site-stable, document-dependent leaks (e.g. a ZK circuit-class selector) rather than trying to hide or generalize them away.
- The M2 reference app ships bare, with no default evidence plug wired in — a plug stays a verifier-side option, never a default consumer.
- `current_date` is coarsened to midnight-UTC client-side before it enters any proof/evidence path; `max_scan_age` floors at 1 day.
- Chip-cloning/clone replay is an accepted, stated limitation (not mitigated) — mode-B uniqueness only holds where `chip_auth` is true.
- zkagent supports both `sig-ed25519/1` and `sig-p256/1` as mode-B evidence plugs (algorithm agility) — device picks the strongest key it supports, verifier accepts either.
- Ed25519 is unavailable via `AndroidKeyStore` on the Pixel 6a — StrongBox rejects it or swaps in P-256 silently; always assert the returned algorithm/curve.
- A device's "biometric" gate can mean biometric-or-credential — a PIN can satisfy it too; verify the real auth factor (`dumpsys fingerprint`), don't assume.
- zkagent's masterlist is the full BSI CMS, signer-chain-verified at load: corrupt/unparseable → `ok:false`; well-formed but lacking the issuer → `ok:true, allowed:false`.
- Release Android builds ship zero cleartext `network_security_config` exceptions; verify via `aapt2 dump` on the APK — release resource names get obfuscated.
- Any screen rendering raw passport/ID fields is unsafe to accessibility-snapshot, not just unsafe to commit — it can leak into an agent's own transcript.
- zkagent's DCQL/mdoc handoff is not interoperable with real EU age-verification wallets as designed — mdoc implies an issuer, which is NO-GO #3.
- A permissively-licensed third-party ZK/age library isn't automatically adoptable — check DSC key-size/ABI coverage, nonce binding, and off-device verifiability first.
- UI code must always log a result/report write (never write it silently); a non-2xx `direct_post` response must map to an explicit user-facing refusal, never read as PASS/success.
- Gitignored fixtures/artefacts don't exist inside a fresh git worktree — copy them in explicitly or dependent tests silently skip or fail.
- To isolate a suspect workflow/config file, A/B test a byte-identical known-good version changing only the one suspected variable — inspection alone leaves it a plausible suspect.
- Always independently verify a subagent's own commit when its completion message carries a security-classifier warning — don't trust its self-report.
- Parse test/build counts yourself from the JUnit XML/build output — never take a pass/fail count from an agent's prose summary.
- Every agent spawn must carry prior findings, relevant git log/decisions, and a FIX-vs-ENHANCEMENT tag in its prompt body — LOC is never the gate.
- To confirm which transaction/session actually completed, check the counterparty's (verifier's) independent state, not just the device's own log.
- Single ownership of a mutable field (WHERE it's written) and fencing late async delivery (WHEN it's applied) are orthogonal — fixing one leaves the other open.
- Enumerate async landings to fence by the hazard predicate ("a late framework callback touching Activity state"), not a syntactic grep like `runOnUiThread`.
- An Activity-lifecycle fence should drop a stale render only, never cancel in-flight work — aborting a request already sent is worse than losing its report.
- Reusing an existing UI mechanism (e.g. a dialog) for a new purpose inherits its side effects — check the contract first; never reuse the outcome dialog for paste refusals (#12).
- Under Android's `isReturnDefaultValues=true` unit-test stubs, View/Spannable/AsyncTask/EditText are non-functional — keep logic in pure classes, Activity thin.
- A unit test pinning only the final state transition can be blind to a bug that changes the user-facing message while landing on the same transition.
- A test that reads its expected value from the same constant as the code under test can only ever agree with it — derive expectations independently.
- A device attester/signing key's scope must be at least as narrow as the identity it signs for — never one key covering multiple documents/zktags at one site.
- An evidence plug's linkability class is a property measured from its actual payload, never inferred from its technology category.
- Blocking modal dialogs are for errors the user must act on; Snackbar is for transient facts with no state change — never blend the two.
- Findings not fixed in the current pass go into the durable findings file, never a code comment — comments get dropped from the next agent's briefing.
- A subagent told to kill/stop a process by name may kill every match including the invoking shell — kill by exact pid, or use `pgrep -f` with an anchored/bracket-trick pattern.
- Verify a claim about framework/library runtime behavior (e.g. does a callback fire post-destroy) against its bytecode/source, not plausible reasoning.
- Before releasing, check the tag convention (plain `vX.Y.Z`, D72) and verify a fresh npm publish against the registry JSON — CLI/local cache can show a stale version.
- A negative test must assert it actually excludes something (count > 0) — a plausible-looking pass can mean the guard matched nothing.
- Two independently-viable solutions to one problem don't resolve a scope decision — that needs an explicit owner call, evidence only narrows options.
- Verify a third-party platform's sharing/ToS/quota constraints before basing an architecture decision on it.
- When a review record's SHA sits on a branch that's since been deleted/squash-merged, review the current branch's own merge-base range instead — the old SHA isn't an ancestor.
- A tool-printed count can mean files or occurrences — reconcile units before treating a mismatch as a real gap.
- A coder subagent that refuses to guess-fix an ambiguous bug is right to refuse — one confirming sentence from the owner/device can validate the hypothesis instead.
- A background subagent can finish all its tool calls and stall with no final message — check its written artefacts/commits before assuming it's stuck.
- An adopter-facing type-correctness gate catches real published-package type bugs (e.g. TS2339) that a JS-only unit-test suite would never surface.
- Repo uses lockstep versioning (D72): one version across chiproof package.json, scanner versionName, one CHANGELOG section; chiproof republishes every release even if unchanged.
- Don't repeatedly nudge the owner to run `/remember` — raise it once if truly needed, then stop; the owner invokes it when ready.
- Device-test instructions to the tester list only actions, in order — never embed timing narration (reads as a cue to act and corrupts the test); expectations go after.
- The app's post-scan dialog shows delivery status only (did the result reach the site) — the site's own verdict is never the app's to surface; state this in the customer guide.
- M3's on-device origin is `http://127.0.0.1:8787` via `adb reverse tcp:8787 tcp:8787`; self-signed HTTPS/hosted pages rejected (a hosted verifier reintroduces NO-GO #3).
- `av://` reaches the scanner only via a VIEW intent (browser/camera tap or `adb am start -a VIEW`) — no in-app paste-first path; manual paste is fallback-of-fallback.
- M3 threshold policy (D74): fixed list {15,16,18,21,60,65}; app locks first-seen threshold per origin, refuses a different one; exact-hostname allowlist only, no wildcards.
- D77/D78: tier-A presentations carry no signature by design (unlinkability tradeoff, not a bug); client identity (FR10) is only attester-key strength without an attestation plug.
- A visual state cue (e.g. dimming) visible only in a before/after transition reads as broken when idle in neither state — give resting-state indicators visible contrast too.
- Cherry-picking multiple worktree branches onto a base reliably conflicts on `docs/index.md`/CHANGELOG — keep both, regenerate the index once at the end, not per-pick.
- A spike's own internal green passes prove nothing about interoperating with a real external system — validate against the real counterpart, not just internal suites.
- A bug that's a disagreement between two internally-consistent contract sides (app vs verifier) is invisible to either side's own test suite — check both halves together.

- A guard needs three proofs: a unit truth table, a source wiring trace, and a real second process hitting the exported surface — only the third catches a guard that never fires.
## Episodes
### 2026-09-02 — M2 fence pass, branch review, chiproof 0.4.0 release
- goal: Fence the unfenced async writer sites (the freeze's remaining criterion), review the 44-commit branch, and close it out.
- tried: Built a pure `LifecycleFence` class, initially fencing 11 sites found by grepping `runOnUiThread`; `/branch-review` found a High-severity gap — `BiometricPrompt` callbacks are also late-delivered main-thread landings but don't match that grep and weren't fenced.
- outcome: Fix applied (`72e0b2c`), re-review returned READY; branch merged to main (PR #4, admin bypass) and `chiproof@0.4.0` published; freeze carried forward as D60 (not lifted) since findings #10/#11 remain open.
- lesson: Enumerating hazard sites by a syntactic grep pattern reproduces exactly the blind spot of whoever wrote that grep — enumerate by the underlying hazard predicate instead.

### 2026-09-02 — M2 reconciliation: PRD split, fix round, D61–D64, unfreeze prep
- goal: Close the 5-fences reconciliation, dedupe the PRD via `/docs-builder`, and run a `/refactor` fix round to clear the remaining findings before lifting the M2 freeze.
- tried: Split the 2,276-line PRD into core spec + wiki pages (milestones/decisions/questions/history) with the original byte-frozen in `docs/archive/`; ran two `/branch-review` passes (one had to review the branch's own merge-base range since the prior record's SHA was on a deleted, squash-merged branch); fixed findings #6 (Snackbar removed) and #8 (pure `ChipAuthClassification` extracted) via a Sonnet coder running in an isolated worktree while a review held the main checkout; fixed Q46's wrong label and investigated Q47 (a `DatePickerDialog` focus-restoration bug), which the coder correctly declined to guess-fix until the owner's one-sentence device account confirmed the hypothesis.
- outcome: PRD split and re-pointed cleanly (1,551/1,551 lines validated, 0 citation violations); a peer session's own bug report on the split's link-rewrite ordering was cross-checked and found a units mismatch (33 files vs 38 occurrences) in its own tooling; findings #6/#8/#10/#11 closed, #17 opened (Q47), D61–D64 recorded; Q47 fix, orientation lock, and the device verification pass were still in flight at stash time, leaving the freeze not yet lifted.
- lesson: A review record's SHA can land on a branch that no longer exists after a squash-merge — reviewing the branch's own merge-base range instead of trusting the recorded SHA is what catches this.

### 2026-09-03 — M2 unfreeze, device session, close-out
- goal: Record owner rulings D61-D64, run a device session validating them, lift the freeze, and release/merge M2 through to main.
- tried: Fixed Q47 focus bug and locked orientation to portrait; ran an 8-scenario device session against two live verifier spikes including a hostile cross-origin `av://` injection and a forced-recreation mid-read test; ran two `/branch-review` passes plus `/release`.
- outcome: All reachable device scenarios passed (one gate proved unreachable live, confirmed unit/wiring-proven only); freeze lifted (D65); merged to main via owner-initiated FF push (bypassing PR-required branch protection, flagged) with no version bump, new finding #18 opened (QR thumbnail/unlogged Snackbars).
- lesson: A background subagent can finish all its tool work and then stall with no final message for hours — check its written artefacts/commits directly rather than trusting its running status.

### 2026-09-03 — M2 exit cleanup round
- goal: Clear all outstanding items from the prior device session before starting the next module, folding in five owner UX decisions (Q36/Q39/Q40/Q43/Q44/Q45/Q48).
- tried: Implemented mint-time threshold refusal, honest under-threshold mint/handoff, an in-app QR scanner that was then removed entirely (D69) after the owner asked "why do I need it inside my app" and confirmed the native Camera app already handles it; fixed findings #19-#21 and ran a two-session device pass across two verifier spikes.
- outcome: All four findings closed/fixed and device-confirmed; mode A was found to never actually deliver a presentation (#21, HIGH) despite prior all-green suites because every earlier device session had only exercised mode B.
- lesson: A test suite (and every prior device session) exercising only one code path (mode B) leaves a HIGH-severity gap in a sibling path (mode A) completely invisible until someone runs that specific path live.

### 2026-09-03 — M2 closed, v0.5.0 released
- goal: Finish remaining glance checks, resolve the last owner UX decisions (D71), and take M2 through review and release for good.
- tried: Owner decided to drop dimming, add a mode-disclosure sentence to the outcome dialog, and keep the existing glyph/wording; ran a `/branch-review high`, then `/release`, then owner questioned why the app and library version separately — leading to lockstep versioning (D72).
- outcome: v0.5.0 tagged and merged (FF push, bypass flagged), chiproof@0.5.0 published to npm and verified against the registry JSON (npm CLI showed stale cache); branch deleted, stash written.
- lesson: `/release` surfacing an ambiguous versioning question rather than deciding it itself produced a real, durable policy decision (D72) that a silent default would have skipped.

### 2026-09-03→04 — M3 PRD scope gate
- goal: Do post-M2 admin (docs reorg, device install, fix-ledger refactor) then scope and PRD-gate the M3 milestone before any build starts.
- tried: Ran `/docs-builder reorg` and `/refactor fix-ledger` (fixed an unscoped `git status --porcelain` dirty-check bug); orchestrator mistakenly chained `/branch-review` after a plain "commit" and was corrected by the owner; drafted M3 PRD sections (§6.3-§6.6) covering scope (tier A/B only, mode C deferred to M3b), threshold policy (D74), paste-field UX (D75), and Play Store track, all owner-approved before any code.
- outcome: PRD merged to main alongside the fix-ledger fix; core PRD's stale "mode A/B" language (should be "tier") flagged but left unfixed as pre-existing drift; owner explicitly told the orchestrator to stop repeatedly nudging `/remember`.
- lesson: Never chain a follow-on skill (e.g. review) after a plain "commit" just because another skill's own instructions suggest it — the owner must name the next step.

### 2026-09-04→05 — M3 POC, page pass, S2/S3 device rounds
- goal: Build and device-verify M3's opening riskiest-assumption POC (duplicate-zktag rejection across a server restart), then iterate the demo page and scanner UI per the approved PRD sections.
- tried: Moved the handoff spike into `apps/demo`, built a JSON file store with atomic writes, verified duplicate-zktag refusal survives a kill+restart on both real documents; built the demo page's tier comparison table and outcome display; built the scanner's S2 (question line) and S3 (pane cleanup) UI, fixed a 409-response bug that had been silently reading as PASS, and ran four device rounds covering wrong-details, card-lift, stale-link, and expired-link negatives.
- outcome: POC passed on both documents across a real restart; page pass and S2/S3 UI device-confirmed; finding #22 (hardcoded "verdict: PASS" report lines) found in flight and left for the owner to decide wording on; nothing merged to main this session, only committed on the feature branch on explicit owner "commit".
- lesson: A verifier's non-2xx response being read as PASS is invisible to a green suite until an on-device negative run against a real refusal path (e.g. HTTP 409 already-used) is exercised.

## Antigens
### High Confidence (loaded — applies every session)
- Never claim code is 'done', 'validated', 'tested', 'fixed', or 'verified' without having actually run real verification (tests/regression/smoke run/end-to-end); state explicitly when something is unverified instead of asserting success. (evidence: 9 sessions, 5 projects — "poc first as you have been full of shit lately, verify that it works", "did you validate all fixes and confirmed that no regression? btw, this is by far one of the worst sessions of zero trust of the amount of times you said you finished and validated then found shit") — ag-001

### Medium Confidence (observing — not loaded)
- (none — no class currently sits at 3-4 distinct sessions)

### Low Confidence (needs more data)
- (none — no class currently sits at exactly 2 sessions)
