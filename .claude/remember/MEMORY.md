# Project Memory
> Auto-generated by /remember. Do not edit manually.

## Facts
- Rung-1 status: M0, M1, and M1b are done (chiproof 0.4.0 unpublished); M2 (scanner build) is in progress — don't reopen M0/M1/M1b or redesign the core.
- `grep` on this host is ugrep 7.5.0 — bounded-repetition regexes fail slowly and look like a hang; parse structured data with python3 instead.
- Never write custom passport-chip parsing code — wrap a vetted library (NFCPassportReader on iOS, JMRTD on Android/desktop).
- zkagent is Android-first: builds on the owner's Fedora box, no Mac/paid Apple account needed; iOS is an explicit non-goal for now.
- zkagent's Android dev device: Pixel, stock ROM, bootloader locked — StrongBox/Play Integrity/patch runway decide it; never GrapheneOS, FRP-locked, or 3a/4a units.
- CPU/secure-enclave material (raw attestation chains too) never enters zktag/identity derivation — roots custody only; a raw chain is a stable per-device ID, breaks unlinkability.
- Evidence-plug/verifier contracts must never throw: `ok:false` forces `allowed:null`, `valid:false` forces `allowed:false` — keep both distinctions everywhere.
- Real passport material and PII never enter git or docs/reports — keep `fixtures/real/`, `*.mrz`, `*.dg1`, `*.sod`, `secrets/` gitignored; record field names, verdicts, timings.
- Commit only when the owner explicitly asks — do not commit PRD/CHANGELOG/doc edits proactively.
- Don't blind-replace `tag`→`zktag` — Apple's "NFC tag-reading entitlement" and RFC 9421's own `tag` param must stay as `tag`.
- zkagent npm package is dropped entirely — only `chiproof` ships from this project; `zkagent@0.0.0` stays published, parked, MIT-mislicensed, not being re-raised.
- `main`'s branch protection (1 review + linear history) can't be met solo — GitHub blocks self-approval, forcing admin bypass each merge; flag every bypass, never assume consent.
- When a stale/broken reference is found after a docs reorg, fix it on sight or flag it — do not assume it's already correct.
- zkagent's agent-delegation layer (rung 2, M4-M5) is frozen until age-verification (rung 1) ships — don't design/discuss it, even if asked casually.
- No feature enters a zkagent milestone unless it is first written into the PRD (NO-GO #10, the scope gate).
- Main/orchestrator session never codes or edits docs itself — always spawn an agent, and it must escalate every design decision back rather than deciding alone.
- Every module's build starts with a POC on real devices/documents aimed at its riskiest assumption, before the easy parts get built.
- Play Integrity tokens are non-transferable/per-app-quota, never a shared voucher — why chiproof uses a pluggable evidence-slot design, not fixed attestation.
- X509 chain verification checks signatures only, not validity dates — always check `notBefore`/`notAfter` explicitly, don't trust `verify()` alone.
- A ZK circuit's public inputs can bind a fresh nonce or expose a stable nullifier, never both — a proof is either challenge-bound or a persistent pseudonym.
- After `/clear`, background agents keep running — check `ListAgents` AND wait for a running agent's reply before spawning a new one for the same task, or two agents race.
- An npm 404 on `PUT` under OIDC has two causes — workflow filename mismatch, or no trusted-publisher binding for that package at all; diagnose which, don't assume.
- Real device/API captures parse differently from synthetic fixtures (wrapper layers, log prefixes) — validate parsers against a real capture, not synthetic data alone.
- No ZK circuit built in this project is "ours" — third-party ZK is a validation-grade evidence plug only; never market v1 as zero-knowledge.
- Use worktree isolation for any agent whose task changes the working checkout, so it doesn't leave the main checkout stranded on a side branch.
- `.claude/remember` and `.claude/stash` stay tracked in this repo — LIBRARY_CONVENTIONS §7 exemption, don't gitignore them.
- Verify a hardware/vendor claim (protocol limits, compatibility) against the actual use case, not against a loosely related signal like "works for a similar product."
- Design-only sessions accumulate unresolved surface faster than they resolve it — writing a design's data/read path out concretely surfaces risks that argument alone misses.
- Mode-B individuals are distinguished by `document_number`, not a derived hash of the full MRZ or DG1.
- Disclose known cross-site-stable, document-dependent leaks (e.g. a ZK circuit-class selector) rather than trying to hide or generalize them away.
- The M2 reference app ships bare, with no default evidence plug wired in — a plug stays a verifier-side option, never a default consumer.
- `current_date` is coarsened to midnight-UTC client-side before it enters any proof/evidence path; `max_scan_age` floors at 1 day.
- Chip-cloning/clone replay is an accepted, stated limitation (not mitigated) — mode-B uniqueness only holds where `chip_auth` is true.
- zkagent supports both `sig-ed25519/1` and `sig-p256/1` as mode-B evidence plugs (algorithm agility) — device picks the strongest key it supports, verifier accepts either.
- Ed25519 is unavailable via `AndroidKeyStore` on the Pixel 6a — StrongBox rejects it or swaps in P-256 silently; always assert the returned algorithm/curve.
- A device's "biometric" gate can mean biometric-or-credential — a PIN can satisfy it too; verify the real auth factor (`dumpsys fingerprint`), don't assume.
- zkagent's masterlist is the full BSI CMS, signer-chain-verified at load: corrupt/unparseable → `ok:false`; well-formed but lacking the issuer → `ok:true, allowed:false`.
- Release Android builds ship zero cleartext `network_security_config` exceptions; verify via `aapt2 dump` on the APK — release resource names get obfuscated.
- Any screen rendering raw passport/ID fields is unsafe to accessibility-snapshot, not just unsafe to commit — it can leak into an agent's own transcript.
- zkagent's DCQL/mdoc handoff is not interoperable with real EU age-verification wallets as designed — mdoc implies an issuer, which is NO-GO #3.
- A permissively-licensed third-party ZK/age library isn't automatically adoptable — check DSC key-size/ABI coverage, nonce binding, and off-device verifiability first.
- UI code must never write a result/report string without also logging it — an unlogged UI-only write makes a real failure look like nothing happened.
- Gitignored fixtures/artefacts don't exist inside a fresh git worktree — copy them in explicitly or dependent tests silently skip or fail.
- To isolate a suspect workflow/config file, A/B test a byte-identical known-good version changing only the one suspected variable — inspection alone leaves it a plausible suspect.
- Always independently verify a subagent's own commit when its completion message carries a security-classifier warning — don't trust its self-report.

## Episodes
### 2026-08-29 — M0 run + PRD v1.6
- goal: Run M0, the top-blocker riskiest-assumption spike, against real passport/ID documents for the first time.
- tried: Forked and stripped a JMRTD-based Kotlin reader app, added a probe for passive/active auth and deterministic candidate derivation, scanned a US passport and an NL ID card twice each with 8 planted negatives.
- outcome: All 4 valid runs and 8 planted negatives passed; US passport has no chip authenticity (clone-replayable), NL ID card has full chip authentication (clone-detectable); PRD advanced to v1.6 with a three-tier disclosure shape.
- lesson: A guard string-matched the wrong issuer DN and silently passed a run that should have voided — verify a negative test actually excludes something (assert count > 0), don't trust a plausible-looking pass.

### 2026-08-29 (evening) — M1 POC: attestation risks #4/#8, Q23
- goal: Before building M1's easy verdict/nonce code, POC its riskiest assumptions — whether attestation is verifiable dependency-free, and whether the attestation payload itself leaks device identity.
- tried: Built a zero-dependency Node attestation-chain verifier and diffed real StrongBox/TEE attestation certs captured twice from the Pixel 6a.
- outcome: Risk #4 held (chains verify with stdlib only); risk #8 confirmed (the raw chain is a stable per-device identifier on both paths), opening Q23 and blocking M1 core; also found expired-root and MRZ-persistence defects.
- lesson: A verifier that only checks certificate signatures, not validity dates, can silently accept an already-expired root — date checks must be explicit, not assumed to be covered by `verify()`.

### 2026-08-30 — M1 Q23 spikes: ZK proof vs Play Integrity
- goal: Resolve Q23 (tier-A unlinkability vs. attestation needing device identity) by evidence rather than argument.
- tried: Relaxed the unlinkability requirement to "nothing stable across sites" (D22), then ran two real spikes on the same phone/documents — a Play Integrity token capture/diff, and a full zkPassport ZK proof composition (DSC→SOD→DG1→age) over both documents.
- outcome: Play Integrity passed the cross-site bar with no device-unique fields; the ZK composition also verified fully offline for both documents in ~16s; owner left the two options open (Play Integrity now, or ZK reversing D1) for the next session.
- lesson: Two independently-viable solutions to the same problem don't resolve a scope decision — that still requires an explicit owner call, evidence only narrows the options.

### 2026-08-30 (late) — M1 D24: evidence slot and bucket B1
- goal: Act on the Q23 decision (D23: Play Integrity) and start building M1 core.
- tried: Investigated how Play Integrity is configured and found tokens are non-transferable and per-app-quota, meaning it cannot be borrowed by third-party adopter sites, invalidating D23; reframed the core as a generic "evidence slot" plug architecture instead of a fixed attestation mechanism.
- outcome: D24 adopted — a pluggable evidence contract (`verifyEvidence` never throws, `ok`/`valid` semantics fixed, linkability-gated per tier); a Sonnet coder started building bucket B1 (`packages/chiproof/`) against this spec.
- lesson: A design decision (D23) built on an unverified platform assumption (Play Integrity is borrowable) collapsed on the very next check — verify a third-party platform's sharing/ToS constraints before basing an architecture decision on it.

### 2026-08-30 (late) — M1 release: chiproof 0.1.0 published
- goal: Finish building M1's verifier core through buckets B1-B4, review it, and ship it.
- tried: One Sonnet coder built the full evidence-slot verifier under the D24 spec, went through code review (1 High finding, fixed) and a security scan (0 findings), then published `chiproof@0.1.0` to npm via OIDC trusted publishing.
- outcome: Published successfully (116 tests passing, zero runtime dependencies) after diagnosing an npm 404 caused by a workflow-filename mismatch (`publish.yaml` vs `publish.yml`); NO-GO #7 reworded so v1 is never described as zero-knowledge and only uses third-party ZK as a validation-grade plug.
- lesson: A `/clear` does not stop background agents — an orchestrator that respawns a coder without checking `ListAgents` first can end up with two agents racing on the same commit.

### 2026-08-30 (late) — M1b probe + chiproof 0.2.0 release
- goal: Run M1b (the mode-A unlinkability probe) on real documents, then land a publish-template backport already in flight.
- tried: Ran 7 `zk-passport/1` presentations plus a bare-mode baseline on real NL/US documents through two independent leak detectors; separately merged a 9-commit adopter-gate/prepack backport that exposed real type bugs in the published `chiproof@0.1.0`.
- outcome: Found one real cross-site-stable, document-dependent leak — the DSC/`id_data` `vk_sha256` circuit-class selector; owner ruled D26 to disclose it rather than hide it. The type-bug fix forced an unplanned `chiproof@0.2.0` release (126 tests); two upstream issues filed against zkPassport's public repos.
- lesson: An adopter-facing type-correctness gate can surface real published-package bugs that unit tests never would — TypeScript consumers hit `TS2339`/silent-wrong-type errors invisible to the JS test suite.

### 2026-08-30 (late) — D27/D28 decisions, zkagent npm dropped
- goal: Close two open PRD questions and resolve the placeholder `zkagent` npm package's fate.
- tried: Owner closed Q25 (D27: M2 ships bare) and Q27 (D28: coarsen `current_date`, floor `max_scan_age`); attempted a `zkagent@0.0.1` placeholder publish, which hit a GitHub Actions dispatch bug then three straight npm E404s.
- outcome: An A/B test with a byte-identical known-good workflow exonerated `publish.yml` and isolated the real cause to npm's per-package trusted-publisher binding (only `chiproof` has one); owner then dropped the placeholder effort entirely and all its commits were reverted.
- lesson: An E404 on `PUT` under OIDC trusted publishing can mean either a workflow filename mismatch or simply no trusted-publisher binding for that package — the two causes look identical and need to be diagnosed separately, not assumed to be the same recurring bug.

### 2026-08-31 — M2 opening POC: handoff + reinstall/masterlist scan
- goal: Run M2's opening riskiest-assumption POCs — a Blueprint-shaped verifier/wallet handoff and a reinstall/masterlist scan — on the Pixel 6a before building the scanner app proper.
- tried: Built a fake EU Blueprint verifier+wallet handoff spike (ES256-signed request object) and ran it device-side (`av://` tap → verify → `direct_post`); separately forked M0 into `m2-scan` with MRZ persistence removed and ran three tests (zktag survival across reinstall, masterlist load/negative, mode-A silence) on both real documents.
- outcome: Handoff worked and correctly refused a tampered request, but checking against the EU's real reference verifier and Android ZK library found zkagent's DCQL is decorative and its trust model differs structurally (NO-GO #3) — not interoperable as designed; all three scan tests passed on both documents; D9 (document_number) and D30 (`sig-ed25519/1` default) were set.
- lesson: Self-consistent green tests prove nothing about real interoperability — a spike's own 15/15 and 17/17 passes said nothing about whether a real EU verifier would accept the payload it sent.

### 2026-08-31 — M2 build: PRD scope gate + Ed25519 key test
- goal: Lock the M2 build scope into the PRD, then run the build's own riskiest-assumption POC (StrongBox keygen + biometric + PACE read in one NFC session).
- tried: Added a 12-item MUST/MUST NOT §6.2 to the PRD; ran the composed POC five times on the Pixel 6a, fixing a SpongyCastle provider-shadowing bug and a verdict-integrity bug along the way; built a zero-NFC "KEY TEST" button to sweep all key/algorithm combinations unattended.
- outcome: Runs 4-5 passed end-to-end on both documents; the KEY TEST found Ed25519 is unavailable via `AndroidKeyStore` on this device (silently substitutes P-256), superseding an earlier "provider gap" reading and correcting an earlier "confirmed" claim; owner also corrected 8 "fingerprint" claims in the evidence doc to "PIN" (dumpsys showed zero enrolled prints); owner resolved F2 as algorithm agility (support both Ed25519 and P-256).
- lesson: A capability probe that only confirms "hardware-backed key succeeded" is insufficient — it must assert the actual algorithm/curve returned, since the distinguishing evidence (91-byte EC key where 44-byte Ed25519 was expected) was in the log all along and got called "confirmed" without comparison.

### 2026-08-31 — M2 build: sig-p256/1 plug + scanner app
- goal: Build the `sig-p256/1` chiproof plug (per F2) and the M2 scanner app through §6.2 items 1-11.
- tried: Built the full `sig-*/1` attester-sig plug family in chiproof (0.4.0, unpublished) and cross-verified it byte-for-byte against the existing handoff spike and four real Pixel 6a StrongBox signatures; built `apps/scanner/`, which minted successfully on a live device run (mode B, P-256/StrongBox, masterlist CMS-verified 588/588) but produced two silent no-report runs.
- outcome: Root-caused the silent runs to a UI code path that wrote `reportView.text` directly without any logging call, explaining all three symptoms (mode A silence, no key lines, verifier receiving nothing) at once; fixed by routing all report writes through one logged `emitReport()`. F5 (an earlier mode-radio display bug) remains open and unreproduced.
- lesson: A UI-only write path with no corresponding log call can make a real, fully-completed run look identical to a hang or crash — always log before or alongside any user-facing status write.

## Antigens
### High Confidence (loaded — applies every session)
- Never claim code is 'done', 'validated', 'tested', 'fixed', or 'verified' without having actually run real verification (tests/regression/smoke run/end-to-end); state explicitly when something is unverified instead of asserting success. (evidence: 8 sessions, 5 projects — "poc first as you have been full of shit lately, verify that it works", "did you validate all fixes and confirmed that no regression? btw, this is by far one of the worst sessions of zero trust of the amount of times you said you finished and validated then found shit") — ag-001

### Medium Confidence (observing — not loaded)
- (none — no class currently sits at 3-4 distinct sessions)

### Low Confidence (needs more data)
- (none — no class currently sits at exactly 2 sessions)
