<?xml version="1.0" encoding="utf-8"?>
<!--
  §6.2 item 10 — DEBUG build only (app/src/debug, overrides app/src/main's
  no-cleartext-anywhere config for debug builds only). Exactly one cleartext
  exception, scoped to 10.0.2.2 (the Android emulator's host-loopback alias)
  and 127.0.0.1/localhost, for the local test verifier
  (spikes/m2-handoff/server.mjs) during development. A release build never
  sees this file — see app/src/main/res/xml/network_security_config.xml.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
